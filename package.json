{
  "name": "sf-admin-cli",
  "private": true,
  "description": "sf admin cli",
  "main": "index.js",
  "scripts": {
    "prepublish": "rimraf commands/**/package-lock.json core/**/package-lock.json",
    "lint": "eslint \"{commands,core,scripts}/**/*.js\" --fix",
    "lint-nofix": "eslint \"{commands,core,scripts}/**/*.js\"",
    "publish": "npm run prepublish && lerna publish",
    "clean": "node ./scripts/clean.js",
    "sync": "node ./scripts/syncTemplate.js"
  },
  "devDependencies": {
    "adm-zip": "^0.5.5",
    "axios": "^0.21.1",
    "eslint": "^7.30.0",
    "eslint-plugin-prettier": "^3.4.0",
    "fs-extra": "^9.1.0",
    "inquirer": "^7.3.3",
    "lerna": "^4.0.0",
    "parse-gitignore": "^1.0.1",
    "prettier": "^2.3.2",
    "rimraf": "^3.0.2"
  }
}
